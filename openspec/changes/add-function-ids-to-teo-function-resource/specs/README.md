# Specs for add-function-ids-to-teo-function-resource

This change does not introduce any new capabilities or modify existing capability requirements. The change only adds an optional field (`function_ids`) to an existing resource for filtering purposes in the read operation.

Since there are no new or modified capabilities, no spec files are required for this change.
