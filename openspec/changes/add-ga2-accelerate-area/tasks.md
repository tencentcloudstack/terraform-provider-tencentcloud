## 1. Service-layer plumbing

- [x] 1.1 Add `DescribeGa2AccelerateAreaById(ctx, gaId, areaId string) (*ga2v20250115.AcceleratorAreas, error)` to `tencentcloud/services/ga2/service_tencentcloud_ga2.go`. Reuse the pattern from `DescribeGa2GlobalAcceleratorById`: build a `DescribeAccelerateAreasRequest`, set `request.GlobalAcceleratorId` **outside** the for-loop (the API has no per-area filter slot); iterate with `Limit=100` (literal, no new constant); strict-equal on `*item.AcceleratorAreaId == areaId`; wrap each SDK page in `resource.Retry(tccommon.ReadRetryTimeout, ...)`; defend against nil `result`/`result.Response` and nil items; return `(nil, nil)` when not found; emit `[CRITAL]` log on retry failure.
- [x] 1.2 Add `DescribeGa2AccelerateAreaByRegion(ctx, gaId, region string) (*ga2v20250115.AcceleratorAreas, error)` to the same file, identical to 1.1 but strict-equal on `*item.AccelerateRegion == region`. Used by Create to resolve the server-generated `AcceleratorAreaId`. (Implemented via a shared private `describeGa2AccelerateAreas` paginator + two thin matchers.)
- [x] 1.3 Confirm `connectivity.UseGa2V20250115Client` is already wired (no change required) and that the SDK package import path remains `github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/ga2/v20250115`.

## 2. Resource implementation

- [x] 2.1 Create `tencentcloud/services/ga2/resource_tc_ga2_accelerate_area.go` (single file). Top-level layout: package + imports → `ResourceTencentCloudGa2AccelerateArea()` schema → `resourceTencentCloudGa2AccelerateAreaCreate/Read/Update/Delete` → `parseGa2AccelerateAreaId` → build helpers (`buildGa2AcceleratorArea`, `buildGa2AccelerateAreaStringSet`) → flatten helpers (`flattenGa2IpAddressInfoSet`).
- [x] 2.2 Schema fields per the spec, in this declaration order: `global_accelerator_id` (Required, ForceNew), `accelerate_region` (Required, ForceNew), `bandwidth` (Optional+Computed Int), `isp_type` (Optional+Computed String), `ip_version` (Optional+Computed String), `ip_address` (Optional+Computed TypeSet of String), and computed-only: `accelerator_area_id` (String), `ip_address_info_set` (Computed TypeList of nested block with `ip_address` + `isp_type` Strings). Add a `Timeouts` block with `Create/Update/Delete: 5 * time.Minute`. Use `schema.ImportStatePassthrough` for the Importer.
- [x] 2.3 Implement `Create`: build `CreateAccelerateAreasRequest` with `GlobalAcceleratorId` and a single-element `AcceleratorAreas` list (translate `bandwidth` via `helper.IntUint64`, convert `ip_address` Set via `(*schema.Set).List()` then to `[]*string`); wrap the SDK call in `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil `Response`/`TaskId`; assert `TaskId != ""` and call `Ga2Service.WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutCreate))`; then call `DescribeGa2AccelerateAreaByRegion(ctx, gaId, region)` to resolve `AcceleratorAreaId` (error if `(nil,nil)` or nil `AcceleratorAreaId`); finally `d.SetId(strings.Join([]string{gaId, areaId}, tccommon.FILED_SP))` and return `Read`.
- [x] 2.4 Implement `Read`: parse composite ID into `(gaId, areaId)` via `parseGa2AccelerateAreaId`; call `DescribeGa2AccelerateAreaById`; on `(nil, nil)` log `[WARN]` and `d.SetId("")` then return; otherwise `_ = d.Set(...)` for `global_accelerator_id`, `accelerate_region`, `bandwidth` (cast `*uint64`→`int`), `isp_type`, `ip_version`, `ip_address`, `accelerator_area_id`, and the flattened `ip_address_info_set`. Use the "set only when non-nil" idiom (no `else` branches).
- [x] 2.5 Implement `Update`: short-circuit (skip Modify) if none of `bandwidth`/`isp_type`/`ip_version`/`ip_address` changed; otherwise build `ModifyAccelerateAreasRequest` with `GlobalAcceleratorId` and a single-element `AcceleratorAreas` list carrying `AcceleratorAreaId` (from parsed ID) + `AccelerateRegion` (from state) + the current mutable field values. Wrap the SDK call in `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil response/`TaskId`; call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutUpdate))`; return `Read`.
- [x] 2.6 Implement `Delete`: call `DeleteAccelerateAreasWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)` with `GlobalAcceleratorId` and `AcceleratorAreaIds=[areaId]` from the parsed composite ID; defend against nil `Response`/`TaskId`; call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutDelete))`.
- [x] 2.7 At the top of every CRUD function, add `defer tccommon.LogElapsed("resource.tencentcloud_ga2_accelerate_area.<op>")()` and `defer tccommon.InconsistentCheck(d, meta)()`. Emit the `[DEBUG]` per-call log line matching the existing GA2 format.
- [x] 2.8 ID parser: `parseGa2AccelerateAreaId(id string) (gaId, areaId string, err error)` — split on `tccommon.FILED_SP`, expect exactly 2 non-empty parts, otherwise return a descriptive error.

## 3. Provider registration

- [x] 3.1 In `tencentcloud/provider.go`, register `"tencentcloud_ga2_accelerate_area": ga2.ResourceTencentCloudGa2AccelerateArea()` in `ResourcesMap`. Place it adjacent to the other `tencentcloud_ga2_*` entries to keep the `ga2` namespace contiguous.
- [x] 3.2 In `tencentcloud/provider.md`, append `tencentcloud_ga2_accelerate_area` to the existing `Global Accelerator(GA2)` Resources section so `gendoc` picks it up on the next `make doc`.

## 4. Documentation example

- [x] 4.1 Create `tencentcloud/services/ga2/resource_tc_ga2_accelerate_area.md` (filename pattern matches `resource_tc_config_compliance_pack.md`). Content sections: short description; HCL example showing `global_accelerator_id`, `accelerate_region`, `bandwidth`, `isp_type`, `ip_version`; an `Import` section showing `terraform import tencentcloud_ga2_accelerate_area.example ga-xxxxxxxx#area-yyyyyyy`. **Do not** hand-edit `website/docs/r/ga2_accelerate_area.html.markdown` — that file is regenerated from the resource Schema/Description by `make doc`.

## 5. Acceptance test scaffolding

- [x] 5.1 Create `tencentcloud/services/ga2/resource_tc_ga2_accelerate_area_test.go` (filename pattern matches `resource_tc_config_compliance_pack_test.go`). Include `TestAccTencentCloudGa2AccelerateAreaResource_basic` with `resource.Test` containing at minimum: a create step, an update step (e.g. change `bandwidth`), and an `ImportState` verification step. Use `tcacctest.AccPreCheck` and the standard provider factories.

## 6. Build & verification

- [x] 6.1 Run `go build ./tencentcloud/...` and confirm clean compilation. (Verified `go build ./tencentcloud/` and `./tencentcloud/services/ga2/` — EXIT 0.)
- [x] 6.2 Run `go vet ./tencentcloud/services/ga2/...` and confirm no new findings. (EXIT 0, no output.)
- [x] 6.3 Run `read_lints` on the changed files and confirm no new ERROR/WARN beyond the project-wide deprecated HINTs (`resource.Retry`, `schema.ImportStatePassthrough`, `d.GetOkExists`). (Only the expected deprecation HINTs present.)
- [x] 6.4 Run `make doc` to regenerate `website/docs/r/ga2_accelerate_area.html.markdown` from the resource Schema/Description and the `.md` example file. Verify the generated doc renders the example block, all input fields, computed fields, the `ip_address_info_set` nested block, and the import syntax. **Do not** hand-edit the generated file. (Generated successfully; `tencentcloud.erb` nav link auto-added. Note: gendoc rejects non-ASCII descriptions, so the `isp_type` Description avoids CJK enum literals.)
- [ ] 6.5 (Optional, requires `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY`) Run the acceptance test: `TF_ACC=1 go test -timeout 30m -run TestAccTencentCloudGa2AccelerateAreaResource_basic ./tencentcloud/services/ga2/...`.
