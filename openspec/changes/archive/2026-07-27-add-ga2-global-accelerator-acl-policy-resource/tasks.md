## 1. Service-layer plumbing

- [x] 1.1 Add `DescribeGa2GlobalAcceleratorAclPolicyById(ctx, gaId, policyId string) (*ga2v20250115.GlobalAcceleratorAclPolicies, error)` to `tencentcloud/services/ga2/service_tencentcloud_ga2.go`. Reuse the existing pattern from `DescribeGa2GlobalAcceleratorById`: build the `DescribeGlobalAcceleratorAclPoliciesRequest` **outside** the for-loop (set `GlobalAcceleratorId` once); iterate with `Limit="200"` (literal `*string`, the documented maximum) and `Offset` (`*uint64`) starting at 0, mutating only `Offset`/`Limit` per page; strict-equal on `*item.GlobalAcceleratorAclPolicyId == policyId`; wrap each SDK page in `resource.Retry(tccommon.ReadRetryTimeout, ...)` with a nil-`Response` defense returning `resource.NonRetryableError`; return `(nil, nil)` when not found; emit `[CRITAL]` log on retry failure.
- [x] 1.2 Confirm `connectivity.UseGa2V20250115Client` is already wired (no change required) and that the SDK package import path remains `github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/ga2/v20250115`. Confirm the four AclPolicy APIs (`CreateGlobalAcceleratorAclPolicy`, `DescribeGlobalAcceleratorAclPolicies`, `ModifyGlobalAcceleratorAclPolicy`, `DeleteGlobalAcceleratorAclPolicy`) plus `DescribeTaskResult` are all present in the vendored SDK before writing resource code.

## 2. Resource implementation

- [x] 2.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_policy.go` (single file, no `_extension.go`). Top-level layout: package + imports → `ResourceTencentCloudGa2GlobalAcceleratorAclPolicy()` schema → `resourceTencentCloudGa2GlobalAcceleratorAclPolicyCreate/Read/Update/Delete` → composite-ID split helper. Do NOT add a comment header at the top of the file.
- [x] 2.2 Define the schema fields in this declaration order: `global_accelerator_id` (TypeString, Required, ForceNew), `default_action` (TypeString, Required, ForceNew), `status` (TypeString, Optional, Computed), `global_accelerator_acl_policy_id` (TypeString, Computed), `task_id` (TypeString, Computed). Add a `Timeouts` block with `Create/Update/Delete: 5 * time.Minute` defaults. Add an `Importer` using `schema.ImportStatePassthrough`.
- [x] 2.3 Implement `Create`: build `CreateGlobalAcceleratorAclPolicyRequest` from `global_accelerator_id` and `default_action`; wrap the SDK call in `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil `Response`, nil/empty `GlobalAcceleratorAclPolicyId`, and nil `TaskId` by returning `resource.NonRetryableError`; after the retry block returns, call `Ga2Service.WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutCreate))`; then `d.SetId(gaId + tccommon.FILED_SP + policyId)` and return `Read`. Ensure `d.SetId` and `task_id` state-setting happen outside the retry closure.
- [x] 2.4 Implement `Read`: split `d.Id()` on `tccommon.FILED_SP` into `(gaId, policyId)` (return a descriptive error if the split does not yield exactly two parts); call `DescribeGa2GlobalAcceleratorAclPolicyById`; on `(nil, nil)` first `log.Printf("[CRUD] ga2_global_accelerator_acl_policy id=%s", d.Id())` then, if `IsNewResource()` return an explicit error else `d.SetId("")` and return nil; otherwise `_ = d.Set(...)` for `global_accelerator_id`, `default_action`, `status`, `global_accelerator_acl_policy_id`, `task_id`, each guarded on the corresponding response field being non-nil.
- [x] 2.5 Implement `Update`: split the composite ID; if `status` has changed, build `ModifyGlobalAcceleratorAclPolicyRequest` with `GlobalAcceleratorId`, `GlobalAcceleratorAclPolicyId`, and `Status`; call `ModifyGlobalAcceleratorAclPolicyWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)` with a nil-`Response`/`TaskId` defense; after the retry block, call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutUpdate))`. `default_action` and `global_accelerator_id` are ForceNew and must NOT be sent on Modify. Skip the Modify call when `status` is unchanged. Finish by returning `Read`.
- [x] 2.6 Implement `Delete`: split the composite ID; call `DeleteGlobalAcceleratorAclPolicyWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)` with a nil-`Response`/`TaskId` defense and a `ResourceNotFound` short-circuit returning nil; after the retry block, call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutDelete))`.
- [x] 2.7 At the top of every CRUD function, add `defer tccommon.LogElapsed("resource.tencentcloud_ga2_global_accelerator_acl_policy.<op>")()` and `defer tccommon.InconsistentCheck(d, meta)()`. Emit a `[DEBUG]` line per SDK invocation (action, request body, response body) and a `[CRITAL]%s ... failed, reason:%+v` line on each retry-block failure. Use the literal lowercase resource name `ga2_global_accelerator_acl_policy` in all log/error messages.
- [x] 2.8 Add the composite-ID split helper (e.g. `parseGa2AclPolicyCompositeId(id string) (gaId, policyId string, err error)`) that splits on `tccommon.FILED_SP` and validates exactly two non-empty parts.

## 3. Provider registration

- [x] 3.1 In `tencentcloud/provider.go`, register `"tencentcloud_ga2_global_accelerator_acl_policy": ga2.ResourceTencentCloudGa2GlobalAcceleratorAclPolicy()` in `ResourcesMap`. Place it adjacent to the existing `tencentcloud_ga2_global_accelerator` / `tencentcloud_ga2_forwarding_policy` entries to keep the `ga2` namespace contiguous.

## 4. Documentation example

- [x] 4.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_policy.md` (filename pattern matches `resource_tc_ga2_global_accelerator.md`). Content sections: a one-line description mentioning the GA2 (Global Accelerator V2) product; an Example Usage HCL block showing `global_accelerator_id` and `default_action` (referencing a `tencentcloud_ga2_global_accelerator` resource); an `Import` section documenting the composite-ID syntax (`terraform import tencentcloud_ga2_global_accelerator_acl_policy.x <global_accelerator_id>#<global_accelerator_acl_policy_id>`). Do NOT add `Argument Reference` or `Attribute Reference` sections (auto-generated by `make doc`). Do NOT hand-edit `website/docs/`.

## 5. Unit tests (gomonkey mocks)

- [x] 5.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_policy_test.go`. Per the project rule for newly-added resources, use `gomonkey` mocks against the cloud-API client methods (NOT the Terraform acceptance suite). Cover at minimum: successful Create (mock `CreateGlobalAcceleratorAclPolicyWithContext` returning `TaskId`+`GlobalAcceleratorAclPolicyId`, mock `DescribeTaskResultWithContext` returning `Status=="SUCCESS"`, assert composite ID is set); successful Read present (mock `DescribeGlobalAcceleratorAclPoliciesWithContext` returning a matching policy, assert fields hydrated); successful Read absent (assert `d.SetId("")` after the `[CRUD]` log); successful Update (mock `ModifyGlobalAcceleratorAclPolicyWithContext` + task SUCCESS on a `status` change); successful Delete (mock `DeleteGlobalAcceleratorAclPolicyWithContext` + task SUCCESS). Tests must be runnable via `go test -gcflags=all=-l ./tencentcloud/services/ga2/...` without `TENCENTCLOUD_SECRET_ID`/`TENCENTCLOUD_SECRET_KEY`.

## 6. Verification

- [x] 6.1 Run `go build ./tencentcloud/...` and confirm clean compilation (no compilation or vet errors related to the new resource). NOTE: this verification is performed by the downstream pipeline, not during code generation.
- [x] 6.2 Run `go test -gcflags=all=-l ./tencentcloud/services/ga2/...` (or the specific new test file) and confirm the gomonkey-based unit tests pass.
- [ ] 6.3 (Finalize stage, via `tfpacer-finalize` skill) Run `gofmt` on changed Go files, run `make doc` to regenerate `website/docs/r/ga2_global_accelerator_acl_policy.html.markdown` from the resource Schema/Description and the `.md` example file, and generate the `.changelog` entry. Do NOT run `gofmt` / `make doc` / create `.changelog` files outside the finalize stage.
