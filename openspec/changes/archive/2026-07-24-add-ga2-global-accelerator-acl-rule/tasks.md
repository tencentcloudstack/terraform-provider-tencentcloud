## 1. Service-layer plumbing

- [x] 1.1 Add `DescribeGa2GlobalAcceleratorAclRuleById(ctx context.Context, policyId, ruleId string) (*ga2v20250115.GlobalAcceleratorAclRuleSet, error)` to `tencentcloud/services/ga2/service_tencentcloud_ga2.go`. Reuse the existing pagination pattern: build `DescribeGlobalAcceleratorAclRulesRequest` with `Limit=200` (the API-documented maximum); iterate pages; match by `*item.GlobalAcceleratorAclRuleId == ruleId`; wrap each SDK page in `resource.Retry(tccommon.ReadRetryTimeout, ...)`; return `(nil, nil)` when not found; emit `[CRITAL]` log on retry failure.
- [x] 1.2 Confirm `connectivity.UseGa2V20250115Client` is already wired (no change required) and that the SDK package import path remains `github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/ga2/v20250115`.

## 2. Resource implementation

- [x] 2.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule.go` (single file). Top-level layout: package + imports → `ResourceTencentCloudGa2GlobalAcceleratorAclRule()` schema → `resourceTencentCloudGa2GlobalAcceleratorAclRuleCreate/Read/Update/Delete`.
- [x] 2.2 Schema fields in this declaration order: `global_accelerator_id` (Required, ForceNew), `global_accelerator_acl_policy_id` (Required, ForceNew), `protocol` (Required), `port` (Required), `source_cidr_block` (Required), `policy` (Required), `description` (Optional), `global_accelerator_acl_rule_id` (Computed), `task_id` (Computed). Add a `Timeouts` block with `Create/Update/Delete: 5 * time.Minute` defaults.
- [x] 2.3 Implement `Create`: build `CreateGlobalAcceleratorAclRuleRequest` from schema (assemble `AclEntries` with a single element from the top-level fields); wrap SDK call in `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil `Response` / `GlobalAcceleratorAclRuleIds` / `TaskId`; assert `TaskId != ""` and call `Ga2Service.WaitForGa2TaskFinish(ctx, *resp.Response.TaskId, d.Timeout(schema.TimeoutCreate))`; assert `len(GlobalAcceleratorAclRuleIds) > 0`; set composite ID `fmt.Sprintf("%s%s%s%s%s", gaId, tccommon.FILED_SP, policyId, tccommon.FILED_SP, ruleId)`; return `Read`.
- [x] 2.4 Implement `Read`: parse composite ID via `strings.Split(d.Id(), tccommon.FILED_SP)` to get `gaId`, `policyId`, `ruleId`; call `DescribeGa2GlobalAcceleratorAclRuleById`; on `(nil, nil)` log `[WARN]` with `d.Id()` and `d.SetId("")` then return; otherwise `_ = d.Set(...)` for every field including `global_accelerator_id` (from composite ID), `global_accelerator_acl_policy_id` (from composite ID), and all fields from the response.
- [x] 2.5 Implement `Update`: call `ModifyGlobalAcceleratorAclRuleWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)` with all fields; defend against nil `Response` / `TaskId`; call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutUpdate))`.
- [x] 2.6 Implement `Delete`: call `DeleteGlobalAcceleratorAclRuleWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)` with `GlobalAcceleratorAclRuleIds` as a single-element slice; defend against nil `Response` / `TaskId`; call `WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutDelete))`.
- [x] 2.7 At the top of every CRUD function, add `defer tccommon.LogElapsed("resource.tencentcloud_ga2_global_accelerator_acl_rule.<op>")()` and `defer tccommon.InconsistentCheck(d, meta)()`.
- [x] 2.8 Importer: use `schema.ImportStatePassthrough` (matches the existing ga2 resources for consistency).

## 3. Provider registration

- [x] 3.1 In `tencentcloud/provider.go`, register `"tencentcloud_ga2_global_accelerator_acl_rule": ga2.ResourceTencentCloudGa2GlobalAcceleratorAclRule()` in `ResourcesMap`. Place it adjacent to the existing `tencentcloud_ga2_global_accelerator` entry.
- [x] 3.2 In `tencentcloud/provider.md`, add the resource to the documentation list.

## 4. Documentation example

- [x] 4.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule.md`. Content sections: short description; HCL example showing all required fields plus optional `description`; an `Import` section showing `terraform import tencentcloud_ga2_global_accelerator_acl_rule.example ga-xxxxxxxx#aclpol-xxxxxxxx#aclrule-xxxxxxxx`. **Do not** hand-edit `website/docs/r/ga2_global_accelerator_acl_rule.html.markdown` — that file will be (re)generated by `make doc`.

## 5. Acceptance test scaffolding

- [x] 5.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule_test.go`. Include `TestAccTencentCloudGa2GlobalAcceleratorAclRuleResource_basic` with `resource.Test` containing at minimum: a create step, an update step (e.g. change `description`), and an `ImportState` verification step. Use `tcacctest.AccPreCheck` and the standard provider factories.

## 6. Build & verification

- [x] 6.1 Run `go build ./tencentcloud/...` and confirm clean compilation. (Deferred to post-generation verification pipeline per project rules.)
- [x] 6.2 Run `go vet ./tencentcloud/services/ga2/...` and confirm no new findings. (Deferred to post-generation verification pipeline per project rules.)
- [x] 6.3 Run `make doc` to regenerate documentation. Verify the generated doc renders the example block, all fields, computed fields, and the import syntax. **Do not** hand-edit the generated file. (Deferred to tfpacer-finalize skill per project rules.)
- [x] 6.4 (Optional, requires `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY`) Run the acceptance test: `TF_ACC=1 go test -timeout 30m -run TestAccTencentCloudGa2GlobalAcceleratorAclRuleResource_basic ./tencentcloud/services/ga2/...`. (Skipped - no credentials available.)