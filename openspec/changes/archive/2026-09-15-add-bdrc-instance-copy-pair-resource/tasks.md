## 1. Connectivity & Service-layer plumbing

- [x] 1.1 在 `tencentcloud/connectivity/client.go` 中新增 bdrc v20260330 包导入：`bdrcv20260330 "github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/bdrc/v20260330"`。在 `TencentCloudClient` 结构体中新增连接字段 `bdrcv20260330Conn *bdrcv20260330.Client`（放置位置参照 `igtmv20231024Conn` 所在区域）。新增客户端工厂方法 `UseBdrcV20260330Client()`，参照 `UseIgtmV20231024Client` 实现：懒初始化、`NewClientProfile(300)`、`WithHttpTransport(&LogRoundTripper{})`。
- [x] 1.2 创建 `tencentcloud/services/bdrc/service_tencentcloud_bdrc.go`，定义 `NewBdrcService(client *connectivity.TencentCloudClient)` 构造函数和 `BdrcService` 结构体（字段 `client *connectivity.TencentCloudClient`）。实现 `DescribeBdrcInstanceCopyPairById(ctx, copyPairId string) (*bdrcv20260330.CopyPair, error)` 方法：构造 `DescribeCopyPairsRequest`，设置 `CopyPairType = "INSTANCE"`、`CopyPairIds = [copyPairId]`；在 `resource.Retry(tccommon.ReadRetryTimeout, ...)` 中调用 `DescribeCopyPairs`；retry 块内若 `response == nil || response.Response == nil || len(response.Response.CopyPairSet) == 0` 则返回 `NonRetryableError`（不清空 id）；retry 失败路径打印 `log.Printf("[CRUD] bdrc_instance_copy_pair id=%s", copyPairId)` 后返回 `(nil, nil)`；找到匹配项后做 `*item.CopyPairId == copyPairId` 严格校验再返回。

## 2. Resource schema & CRUD implementation

- [x] 2.1 创建 `tencentcloud/services/bdrc/resource_tc_bdrc_instance_copy_pair.go`（单文件）。顶部布局：package + imports（`context`、`fmt`、`log`、`strings`、`resource`、`schema`、`bdrcv20260330`、`tccommon`、`helper`）→ `ResourceTencentCloudBdrcInstanceCopyPair()` schema 定义 → Create/Read/Update/Delete 函数 → 辅助构建/扁平化函数。
- [x] 2.2 定义 Schema，按以下顺序声明顶层字段：`protect_group_id`（Required, ForceNew）、`create_target_instance_parameters`（Required, ForceNew, TypeList, MaxItems=1, MinItems=1, 嵌套 Resource）、`instance_copy_pair_name`（Optional, 可更新）、`client_token`（Optional, ForceNew）、`recovery_point_objective`（Optional, ForceNew, TypeInt）、`delete_target_resource`（Optional, TypeBool）。`create_target_instance_parameters` 嵌套 Resource 包含 `source_instance_id`（Required）、`instance_charge_type`（Required）、`placement`（Required, TypeList, MaxItems=1, 嵌套 zone/project_id/host_id/host_ids/project_name）、`image_id`（Required）、`system_disk`（Required, TypeList, MaxItems=1, 嵌套 disk_type/disk_size/delete_with_instance）、`instance_charge_prepaid`（Optional, TypeList, MaxItems=1, 嵌套 period/renew_flag）、`instance_type`（Optional）、`data_disks`（Optional, TypeList, 嵌套 disk_type/disk_size/delete_with_instance）、`virtual_private_cloud`（Optional, TypeList, MaxItems=1, 嵌套 vpc_id/subnet_id/subnet_name/as_vpc_gateway/private_ip_addresses/vpc_name/ipv6address_count）、`internet_accessible`（Optional, TypeList, MaxItems=1, 嵌套 internet_charge_type/internet_max_bandwidth_out/public_ip_assigned/internet_service_provider）、`instance_name`（Optional）、`login_settings`（Optional, TypeList, MaxItems=1, 嵌套 password[Sensitive]/key_ids/keep_image_login）、`enhanced_service`（Optional, TypeList, MaxItems=1, 嵌套 security_service/monitor_service/automation_service/basic_service，各为 TypeList MaxItems=1 含 enabled bool）、`spot_price`（Optional）、`host_name`（Optional）、`user_data`（Optional）、`disaster_recover_group_ids`（Optional, TypeList）、`stopped_mode`（Optional）、`copy_pair_id`（Optional）、`recovery_time`（Optional）。
- [x] 2.3 定义 Computed 字段：`copy_pair_ids`（TypeList, Computed）、`copy_pair_id`（Computed）、`copy_pair_name`（Computed）、`copy_pair_state`（Computed）、`copy_pair_type`（Computed）、`site_pair_id`（Computed）、`site_pair_name`（Computed）、`protect_group_name`（Computed）、`source_region`（Computed）、`source_zone`（Computed）、`source_vpc`（Computed）、`target_region`（Computed）、`target_zone`（Computed）、`target_vpc`（Computed）、`source_resource_id`（Computed）、`target_resource_id`（Computed）、`instance_id`（Computed）、`instance_copy_pair_id`（Computed）、`percent`（Computed, TypeInt）、`latest_protection_time`（Computed）、`data_direction`（Computed）、`create_from`（Computed）、`disaster_recovery_type`（Computed）、`peer_cloud_name`（Computed）、`rollbacking`（Computed, TypeInt）、`rollback_percent`（Computed, TypeInt）、`create_time`（Computed）、`account_uin`（Computed）、`sub_account_uin`（Computed）、`drill_group_id`（Computed）、`protection_time_set`（Computed, TypeList）、`disk_copy_pair_set`（Computed, TypeList, 嵌套 copy_pair_id/copy_pair_name/source_resource_id/target_resource_id/create_time）、`deferred_create`（Computed, TypeBool）、`target_cvm_created`（Computed, TypeBool）、`cvm_create_params`（Computed）。设置 `Importer: &schema.ResourceImporter{State: schema.ImportStatePassthrough}`。
- [x] 2.4 实现 `resourceTencentCloudBdrcInstanceCopyPairCreate`：顶部 `defer tccommon.LogElapsed("resource.tencentcloud_bdrc_instance_copy_pair.create")()` + `defer tccommon.InconsistentCheck(d, meta)()`。构造 `CreateInstanceCopyPairRequest`：从 schema 读取 `protect_group_id`、`instance_copy_pair_name`、`client_token`、`recovery_point_objective` 设置到 request；从 `create_target_instance_parameters` 列表构建 `[]*CreateInstanceModel`（遍历列表元素，为每个元素构建 `CreateInstanceModel` 及其嵌套子结构 `Placement`/`SystemDisk`/`InstanceChargePrepaid`/`DataDisks`/`VirtualPrivateCloud`/`InternetAccessible`/`LoginSettings`/`EnhancedService`）。在 `resource.Retry(tccommon.WriteRetryTimeout, ...)` 中调用 `CreateInstanceCopyPairWithContext`，nil Response 防御返回 `NonRetryableError`。retry 成功后检查 `CopyPairIds` 是否为空/nil，打印 `logId` 和 `d.Id()` 便于排障，若空则返回 `NonRetryableError`。取 `CopyPairIds[0]` 作为资源 ID，`d.SetId(*copyPairId)`。异步轮询：调用内部轮询函数循环 `DescribeCopyPairs` 直到 `CopyPairState != "INIT"`（使用 `resource.Retry` 包裹）。最后调用 Read。
- [x] 2.5 实现 `resourceTencentCloudBdrcInstanceCopyPairRead`：顶部 `defer LogElapsed("...read")` + `defer InconsistentCheck(d, meta)`。创建 `BdrcService`，调用 `DescribeBdrcInstanceCopyPairById(ctx, d.Id())`。若返回 `(nil, nil)`：`log.Printf("[CRUD] bdrc_instance_copy_pair id=%s", d.Id())` 后 `d.SetId("")` 返回 nil。若找到：对 `CopyPair` 每个非 nil 字段调用 `_ = d.Set(...)`，包括 `copy_pair_id`、`copy_pair_name`、`copy_pair_state`、`protect_group_id`、`source_region`、`target_region`、`percent`、`recovery_point_objective`、`create_time` 等全部 computed 字段；`disk_copy_pair_set` 列表需遍历 `DiskCopyPairSet` 构建嵌套 map 列表后 `d.Set`。
- [x] 2.6 实现 `resourceTencentCloudBdrcInstanceCopyPairUpdate`：顶部 defer 日志。`mutableArgs := []string{"instance_copy_pair_name"}`。检测 `d.HasChange` 遍历所有非 ForceNew 字段外的顶层字段是否变更，若发现 immutable 字段变更则返回 error。若 `instance_copy_pair_name` 变更：构造 `ModifyCopyPairAttributeRequest`，设置 `CopyPairId = d.Id()`、`CopyPairType = "INSTANCE"`、`CopyPairName`，在 `resource.Retry(tccommon.WriteRetryTimeout, ...)` 中调用 `ModifyCopyPairAttributeWithContext`，nil Response 防御。最后调用 Read。
- [x] 2.7 实现 `resourceTencentCloudBdrcInstanceCopyPairDelete`：顶部 defer 日志。构造 `DeleteCopyPairsRequest`，设置 `CopyPairIds = [d.Id()]`（`[]*string{helper.String(d.Id())}`）、`CopyPairType = "INSTANCE"`，若 schema 中 `delete_target_resource` 已设置则传 `DeleteTargetResource`。在 `resource.Retry(tccommon.WriteRetryTimeout, ...)` 中调用 `DeleteCopyPairsWithContext`，nil Response 防御返回 `NonRetryableError`。

## 3. Provider registration

- [x] 3.1 在 `tencentcloud/provider.go` 中新增 bdrc 服务包导入：`bdrc "github.com/tencentcloudstack/terraform-provider-tencentcloud/tencentcloud/services/bdrc"`（参照 igtm 导入位置）。在 `ResourcesMap` 中注册 `"tencentcloud_bdrc_instance_copy_pair": bdrc.ResourceTencentCloudBdrcInstanceCopyPair()`（放置在 Resource map 中合适位置）。
- [x] 3.2 在 `tencentcloud/provider.md` 中新增 `Backup and Disaster Recovery(BDRC)` 章节标题，其下新增 `Resource` 子标题，列出 `tencentcloud_bdrc_instance_copy_pair` 资源条目（参照 igtm 的 provider.md 格式）。

## 4. Documentation

- [x] 4.1 创建 `tencentcloud/services/bdrc/resource_tc_bdrc_instance_copy_pair.md`。内容包含：(1) 一句话描述，带上所属云产品名称 BDRC，格式 "Provides a resource to create a BDRC instance copy pair"。(2) Example Usage 部分，提供完整的 HCL 示例，展示 `protect_group_id`、`create_target_instance_parameters`（含嵌套 placement/system_disk/virtual_private_cloud 等）等主要参数。(3) Import 部分，说明使用 `terraform import tencentcloud_bdrc_instance_copy_pair.example cvmcopypair-xxxxxxxx`。不添加 Argument Reference 和 Attribute Reference 部分。

## 5. Unit tests

- [x] 5.1 创建 `tencentcloud/services/bdrc/resource_tc_bdrc_instance_copy_pair_test.go`。使用 gomonkey mock 方式（不使用 terraform 测试套件）。mock `meta.(tccommon.ProviderMeta).GetAPIV3Conn().UseBdrcV20260330Client()` 返回的 client 的 `CreateInstanceCopyPairWithContext`/`DescribeCopyPairsWithContext`/`ModifyCopyPairAttributeWithContext`/`DeleteCopyPairsWithContext` 方法。编写测试用例覆盖：Create 成功路径（含异步轮询 mock）、Read 成功路径、Read 资源不存在路径、Update 名称变更路径、Delete 成功路径。确保所有函数返回的 error 被正确处理，必不出错函数用 `_ = func()` 忽略 error。
