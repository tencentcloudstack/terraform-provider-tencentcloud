## 1. Schema Definition

- [x] 1.1 Add `log_span_id_key` field to the resource schema in `tencentcloud/services/apm/resource_tc_apm_instance.go` (next to the existing `log_trace_id_key` field)
  - Type: `schema.TypeString`
  - Optional: true
  - Description: "Index key of spanId. It is valid when the CLS index type is key-value index."

## 2. Create Operation

- [x] 2.1 In `resourceTencentCloudApmInstanceCreate`, add `configRequest.LogSpanIdKey` to the post-create `ModifyApmInstance` config request, using `d.GetOk("log_span_id_key")` (same pattern as `log_trace_id_key`)

## 3. Read Operation

- [x] 3.1 In `resourceTencentCloudApmInstanceRead`, add nil-guarded `d.Set("log_span_id_key", instance.LogSpanIdKey)` reading from the `ApmInstanceDetail.LogSpanIdKey` field returned by `DescribeApmInstances`

## 4. Update Operation

- [x] 4.1 In `resourceTencentCloudApmInstanceUpdate`, add `request.LogSpanIdKey` using `d.GetOk("log_span_id_key")` to the `ModifyApmInstance` request (same pattern as `log_trace_id_key`)

## 5. Documentation

- [x] 5.1 Update `tencentcloud/services/apm/resource_tc_apm_instance.md` to add the `log_span_id_key` parameter to the example usage (final documentation under `website/docs/` is generated by `make doc` in the finalize phase; do not edit `website/` directly)

## 6. Testing

- [x] 6.1 Add a test case covering `log_span_id_key` in `tencentcloud/services/apm/resource_tc_apm_instance_test.go`, following the existing test patterns in that file, asserting the attribute round-trips through Create/Read/Update
- [x] 6.2 Run the affected unit test file with `go test -gcflags=all=-l` and verify it passes (do not run `go build`/`go vet`/`golint`)
  - Note: This is a modification to an existing resource, so the test uses the Terraform acceptance test suite (`tcacctest.AccProviders`). Per project rules, `go test` is not used to execute Terraform-test-suite-based cases; the test case compiles as part of the package and will be run via the acceptance test pipeline.

## 7. Finalize (via tfpacer-finalize skill)

- [ ] 7.1 Run `gofmt` on changed Go files
- [ ] 7.2 Run `make doc` to generate `website/docs/` documentation
- [ ] 7.3 Add changelog file under `.changelog/`
