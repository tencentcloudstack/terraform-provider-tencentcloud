## 1. Preparation

- [x] 1.1 Verify `vendor/github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/dbdc/v20201029/` package contains `DescribeDBCustomRegions` in `client.go` and `DescribeDBCustomRegionsRequest`/`DescribeDBCustomRegionsResponse`/`RegionInfo` in `models.go`
- [x] 1.2 Confirm `DescribeDBCustomRegionsRequest` has no input fields and `DescribeDBCustomRegionsResponseParams` has no pagination fields (only `RegionSet` and `RequestId`)

## 2. Service Layer Implementation

- [x] 2.1 Add `DescribeDBCustomRegions` method to `tencentcloud/services/dbdc/service_tencentcloud_dbdc.go` (append to existing file, do NOT create a new service file)
- [x] 2.2 In `DescribeDBCustomRegions`: build request via `dbdcv20201029.NewDescribeDBCustomRegionsRequest()` (no params), wrap API call `me.client.UseDbdcV20201029Client().DescribeDBCustomRegions(request)` inside `resource.Retry(tccommon.ReadRetryTimeout, ...)` with `ratelimit.Check(request.GetAction())`
- [x] 2.3 In the retry block: if `result == nil || result.Response == nil || result.Response.RegionSet == nil`, log `log.Printf("[DATASOURCE] read empty, skip SetId")` and return `resource.NonRetryableError(fmt.Errorf("Describe dbdc_db_custom_regions failed, Response is nil or empty."))`; otherwise set response and return nil
- [x] 2.4 Do NOT implement pagination loop (the API has no pagination fields); return `ret []*dbdcv20201029.RegionInfo` directly from the single API call result
- [x] 2.5 Add `defer` error logging using `log.Printf("[CRITAL]%s api[%s] fail ...")` with resource name `dbdc_db_custom_regions`

## 3. Datasource Schema & Read Function

- [x] 3.1 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_regions.go` with `DataSourceTencentCloudDbdcDbCustomRegions()` function
- [x] 3.2 Define schema: input param `result_output_file` (Optional, TypeString)
- [x] 3.3 Define schema: computed output `region_set` (TypeList, Computed) with flattened RegionInfo fields: `region` (TypeString, Computed), `region_state` (TypeString, Computed)
- [x] 3.4 Implement `dataSourceTencentCloudDbdcDbCustomRegionsRead` function with `defer tccommon.LogElapsed(...)`, `defer tccommon.InconsistentCheck(d, meta)`, `logId`, `ctx`, and `service = DbdcService{client: meta.(tccommon.ProviderMeta).GetAPIV3Conn()}`
- [x] 3.5 In Read function: wrap `service.DescribeDBCustomRegions(ctx)` call inside `resource.Retry(tccommon.ReadRetryTimeout, ...)`; on error wrap with `tccommon.RetryError(e)`
- [x] 3.6 In Read function: after successful API call, iterate `region_set` with nil checks before calling set methods for each field (`Region` → `region`, `RegionState` → `region_state`)
- [x] 3.7 In Read function: set `d.SetId(helper.BuildToken())` after data population
- [x] 3.8 In Read function: handle `result_output_file` with `tccommon.WriteToFile` if specified
- [x] 3.9 Do NOT add a comment at the top of the resource go file

## 4. Provider Registration

- [x] 4.1 Add `"tencentcloud_dbdc_db_custom_regions": dbdc.DataSourceTencentCloudDbdcDbCustomRegions()` to `tencentcloud/provider.go` DataSourcesMap (near the existing dbdc datasource entries)
- [x] 4.2 Verify the dbdc import already exists in `tencentcloud/provider.go` (line 66)
- [x] 4.3 Add data source entry in `tencentcloud/provider.md` under the DB Custom section

## 5. Documentation

- [x] 5.1 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_regions.md` with: one-line description ("Use this data source to query the supported DB Custom region list from the TencentCloud DBDC product"), Example Usage section (basic query with empty block)
- [x] 5.2 Do NOT add Argument Reference or Attribute Reference sections (auto-generated by make doc)
- [x] 5.3 Do NOT add an Import section (this is a RESOURCE_KIND_DATASOURCE, no Import)

## 6. Unit Testing

- [x] 6.1 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_regions_test.go` in `package dbdc_test` using gomonkey-based mock pattern
- [x] 6.2 Reuse existing `mockMetaDbdcDS`, `ptrStr`, `ptrInt64` helper functions from `data_source_tc_dbdc_db_custom_clusters_test.go` (same package)
- [x] 6.3 Implement `TestDbdcDbCustomRegionsDS_ReadBasic`: mock `DescribeDBCustomRegions` returning two RegionInfo (region + region_state), verify `region_set` has 2 entries with correct values and `d.Id()` is not empty
- [x] 6.4 Implement `TestDbdcDbCustomRegionsDS_ReadWithNilFields`: mock returning a RegionInfo with nil RegionState, verify `region_state` defaults to empty string and `region` is set
- [x] 6.5 Implement `TestDbdcDbCustomRegionsDS_ReadWithEmptyResponse`: mock returning empty RegionSet, verify error is returned (NonRetryableError propagated)
- [x] 6.6 Implement `TestDbdcDbCustomRegionsDS_Schema`: verify schema structure (region_set TypeList Computed, result_output_file, region/region_state Computed TypeString)
- [x] 6.7 Verify tests are runnable with `go test ./tencentcloud/services/dbdc/ -run "TestDbdcDbCustomRegionsDS" -v -count=1 -gcflags="all=-l"` (visual check, do NOT run the command)

## 7. Code Quality Verification

- [x] 7.1 Verify no `go build`/`go vet`/`golint` errors in all new/modified files (visual check, no build command)
- [x] 7.2 Verify nil checks are present for all pointer fields (`Region`, `RegionState`) before set calls
- [x] 7.3 Verify resource name `dbdc_db_custom_regions` is used consistently in log/error messages
- [x] 7.4 Verify no comment at top of resource go files
- [x] 7.5 Verify no `_extension.go` file created (not needed)
- [x] 7.6 Verify the `DescribeDBCustomRegions` API path `response.Response.RegionSet` maps to schema field `region_set` correctly
