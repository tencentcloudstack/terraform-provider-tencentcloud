## 1. Service Layer

- [x] 1.1 Add `DescribeClsMachineGroupsByFilter` method to `tencentcloud/services/cls/service_tencentcloud_cls.go` that builds a `cls.NewDescribeMachineGroupsRequest()`, parses `Filters` from the param map, and paginates (limit=100, offset increments) inside a `for` loop with `resource.Retry(tccommon.ReadRetryTimeout, ...)` calling `me.client.UseClsClient().DescribeMachineGroups(request)`. Inside the retry block, check for `result == nil || result.Response == nil` and return `resource.NonRetryableError`. Return `[]*cls.MachineGroupInfo` and `totalCount`.
- [x] 1.2 Add the `ratelimit.Check(request.GetAction())` call and the defer error-logging block in the service method, consistent with other CLS service methods.

## 2. Data Source Implementation

- [x] 2.1 Create `tencentcloud/services/cls/data_source_tc_cls_machine_groups.go` with `DataSourceTencentCloudClsMachineGroups()` returning the `schema.Resource` (Read function only).
- [x] 2.2 Define the input schema: `filters` (Optional, TypeList of `name` TypeString Required + `values` TypeSet of TypeString Required) and `result_output_file` (Optional, TypeString).
- [x] 2.3 Define the computed output schema: `machine_groups` (TypeList) with flattened element fields `group_id`, `group_name`, `machine_group_type` (TypeList of one: `type`, `values` set), `create_time`, `tags` (list of key/value), `auto_update`, `update_start_time`, `update_end_time`, `service_logging`, `delay_cleanup_time`, `meta_tags` (list of key/value), `os_type`; plus top-level computed `total_count`.
- [x] 2.4 Implement `dataSourceTencentCloudClsMachineGroupsRead` with `defer tccommon.LogElapsed(...)` and `defer tccommon.InconsistentCheck(d, meta)`, build the `paramMap` from `filters`, call `service.DescribeClsMachineGroupsByFilter` inside `resource.Retry(tccommon.ReadRetryTimeout, ...)`, and on the exhausted path log `[DATASOURCE] read empty, skip SetId` and return the error (do NOT call `d.SetId("")`).
- [x] 2.5 Map each `MachineGroupInfo` to the output list with nil-safe checks (skip nested attributes when `MachineGroupType`/`Tags`/`MetaTags` are nil), set `machine_groups`, `total_count`, `d.SetId(helper.DataResourceIdsHash(ids))`, and write `result_output_file` when provided.

## 3. Provider Registration

- [x] 3.1 Register `tencentcloud_cls_machine_groups` data source in `tencentcloud/provider.go` (data source map), following the pattern of `tencentcloud_cls_machine_group_configs`.
- [x] 3.2 Add the data source entry to `tencentcloud/provider.md`.

## 4. Documentation

- [x] 4.1 Create `tencentcloud/services/cls/data_source_tc_cls_machine_groups.md` with a one-line description mentioning CLS, Example Usage (query all + filter by machineGroupName), no Argument/Attribute Reference sections (auto-generated by `make doc`).

## 5. Tests

- [x] 5.1 Create `tencentcloud/services/cls/data_source_tc_cls_machine_groups_test.go` using gomonkey to mock the CLS `DescribeMachineGroups` API (no terraform test suite).
- [x] 5.2 Add a unit test verifying field mapping for a fully-populated `MachineGroupInfo` (including nested `machine_group_type`, `tags`, `meta_tags`).
- [x] 5.3 Add a unit test verifying nil-safe handling when nested fields are nil.
- [x] 5.4 Add a unit test verifying multi-page pagination aggregation (two pages, then a partial page).

## 6. Validation

- [x] 6.1 Verify the data source file and service method compile correctly against the vendored CLS SDK (`DescribeMachineGroups`, `MachineGroupInfo`, `MachineGroupTypeInfo`, `Filter`, `Tag`, `MetaTagInfo`).
- [x] 6.2 Verify all `MachineGroupInfo` fields used in the schema exist in the SDK struct and types match (string/bool/int64/uint64).
- [x] 6.3 Verify provider registration compiles and the data source is wired correctly.
