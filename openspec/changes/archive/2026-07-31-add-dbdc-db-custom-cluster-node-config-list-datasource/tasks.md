## 1. Service Layer

- [x] 1.1 Append `DescribeDBCustomClusterNodeConfigByFilter(ctx, param map[string]interface{}) (ret []*dbdcv20201029.DBCustomClusterNodeConfig, errRet error)` to `tencentcloud/services/dbdc/service_tencentcloud_dbdc.go` — wraps `DescribeDBCustomClusterNodeConfig` with `resource.Retry(tccommon.ReadRetryTimeout)` + `ratelimit.Check`; reads `ClusterId` (`*string`) and `NodeIds` (`[]*string`) from param map; returns `ret = response.Response.NodeSet`; on `errRet != nil` logs `[CRITAL]` with logId/action/request/reason; on success logs `[DEBUG]` with request/response JSON
- [x] 1.2 Verify `UseDbdcV20201029Client()` and SDK types `DescribeDBCustomClusterNodeConfigRequest`/`Response`/`DBCustomClusterNodeConfig`/`Label`/`Taint` exist in vendored `github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/dbdc/v20201029` (no SDK upgrade required)

## 2. Data Source Implementation

- [x] 2.1 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_cluster_node_config_list.go` with `DataSourceTencentCloudDbdcDbCustomClusterNodeConfigList()` returning `*schema.Resource` (Read only). Schema: `cluster_id` (Required String), `node_ids` (Optional List of String), `result_output_file` (Optional String), `node_set` (Computed List, Elem Resource)
- [x] 2.2 Define flattened `node_set` element schema: `node_id` (Computed String), `labels` (Computed List of Resource with `key`/`value` Computed String), `taints` (Computed List of Resource with `key`/`value`/`effect` Computed String). No extra `xxx_set` nesting layer
- [x] 2.3 Implement `dataSourceTencentCloudDbdcDbCustomClusterNodeConfigListRead`: defer `tccommon.LogElapsed` + `tccommon.InconsistentCheck`; build paramMap (`ClusterId` from `cluster_id`, `NodeIds` from `node_ids` via `helper.String`); call `service.DescribeDBCustomClusterNodeConfigByFilter` inside `resource.Retry(tccommon.ReadRetryTimeout, ...)` — on error return `tccommon.RetryError(e)`; on empty response (`result == nil || len(result) == 0`) return `resource.NonRetryableError` (do NOT clear id); after success flatten `NodeSet` into `node_set` with nil guards on `Labels`/`Taints`; `d.SetId(helper.BuildToken())`; handle `result_output_file` via `tccommon.WriteToFile`
- [x] 2.4 Ensure the retry block ONLY contains the API call; setting id / flattening / `_ = d.Set(...)` happen outside the retry block, after error handling. On the failure path emit `log.Printf("[DATASOURCE] read empty, skip SetId")`

## 3. Provider Registration

- [x] 3.1 Register `tencentcloud_dbdc_db_custom_cluster_node_config_list` in `tencentcloud/provider.go` DataSourcesMap: `"tencentcloud_dbdc_db_custom_cluster_node_config_list": dbdc.DataSourceTencentCloudDbdcDbCustomClusterNodeConfigList()`

## 4. Documentation & Tests

- [x] 4.1 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_cluster_node_config_list.md` — one-line description starting with "Use this data source to query ..." (mentions dbdc product); Example Usage section (use `jsonencode()` if JSON string fields arise); no `Argument Reference` / `Attribute Reference` sections (auto-generated by `make doc`)
- [x] 4.2 Create `tencentcloud/services/dbdc/data_source_tc_dbdc_db_custom_cluster_node_config_list_test.go` using `gomonkey` mocks (NOT the Terraform test suite) — mock `UseDbdcV20201029Client` and `DescribeDBCustomClusterNodeConfig`; cover: basic read with 2 nodes (labels + taints), schema validation, empty response returns NonRetryableError; assert `node_set` flattened fields; follow the `data_source_tc_dbdc_db_custom_cluster_nodes_test.go` mock-meta pattern

## 5. Verification

- [x] 5.1 Verify all function error returns are checked; functions that cannot error assign `err` to `_` (e.g. `_ = d.Set(...)`)
- [x] 5.2 Verify the generated `*.go` code compiles in the current environment (cross-check param names against vendored SDK: `request.ClusterId`, `request.NodeIds`, `response.Response.NodeSet`, `DBCustomClusterNodeConfig.NodeId/Labels/Taints`, `Label.Key/Value`, `Taint.Key/Value/Effect`)
- [x] 5.3 Confirm no `go build` / `go vet` / `golint` executed during this phase (gofmt + `make doc` deferred to the tfpacer-finalize skill)
