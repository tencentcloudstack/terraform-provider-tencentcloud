## 1. Schema Changes

- [x] 1.1 Add `network_mode` schema field (TypeString, Computed, description referencing `NetworkModePrivateLink`/`NetworkModeCrossTenantENI` values) to `ResourceTencentCloudDbdcDbCustomNode()` in `tencentcloud/services/dbdc/resource_tc_dbdc_db_custom_node.go`, placed in the computed block (after `isolated_time`, before `system_disk`)
- [x] 1.2 Add `eni_ip` schema field (TypeString, Computed, description noting it is the access IP for `NetworkModeCrossTenantENI` mode) to `ResourceTencentCloudDbdcDbCustomNode()`, placed adjacent to `network_mode`

## 2. Read Function Changes

- [x] 2.1 In `resourceTencentCloudDbdcDbCustomNodeRead`, add `if respData.NetworkMode != nil { _ = d.Set("network_mode", respData.NetworkMode) }` with the other computed string sets
- [x] 2.2 In `resourceTencentCloudDbdcDbCustomNodeRead`, add `if respData.EniIP != nil { _ = d.Set("eni_ip", respData.EniIP) }` with the other computed string sets

## 3. Tests

- [x] 3.1 Add unit test coverage in `tencentcloud/services/dbdc/resource_tc_dbdc_db_custom_node_test.go` for the `network_mode` and `eni_ip` read fields (mock the `DescribeDBCustomNodes` API via gomonkey, assert both fields are set when the response includes them, and skipped when nil)

## 4. Documentation

- [x] 4.1 Update `tencentcloud/services/dbdc/resource_tc_dbdc_db_custom_node.md` to document the new `network_mode` and `eni_ip` computed fields (do NOT add `Argument Reference` / `Attribute Reference` sections; those are auto-generated by `make doc`)

## 5. Validation

- [x] 5.1 Verify the code compiles successfully (build/lint handled outside this step)
- [x] 5.2 Verify no lint errors (handled outside this step)
