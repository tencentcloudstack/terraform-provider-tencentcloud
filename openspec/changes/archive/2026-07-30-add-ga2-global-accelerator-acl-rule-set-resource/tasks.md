## 1. Service-layer plumbing

- [x] 1.1 Add `DescribeGa2GlobalAcceleratorAclRulesByPolicyId(ctx, policyId string) ([]*ga2v20250115.GlobalAcceleratorAclRuleSet, error)` to `tencentcloud/services/ga2/service_tencentcloud_ga2.go`. Reuse the paging pattern from `DescribeGa2GlobalAcceleratorAclRuleById`: build the `DescribeGlobalAcceleratorAclRulesRequest` with `GlobalAcceleratorAclPolicyId` set **outside** the for-loop; iterate with `Limit=200` (literal, the documented maximum) and `Offset` advancing per page; wrap each SDK page in `resource.Retry(tccommon.ReadRetryTimeout, ...)`; return the aggregated slice of all `GlobalAcceleratorAclRuleSet` items across pages; stop when the current page has fewer than `limit` items. If `response.Response` is nil return an error (do not silently return an empty slice). Emit `[CRITAL]` log on retry failure.
- [x] 1.2 Confirm `connectivity.UseGa2V20250115Client` is already wired (no change required) and that the SDK package import path remains `github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/ga2/v20250115`. Confirm `Ga2Service.WaitForGa2TaskFinish` already exists and is reused verbatim.

## 2. Resource implementation

- [x] 2.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule_set.go` (single file). Top-level layout: package + imports → `ResourceTencentCloudGa2GlobalAcceleratorAclRuleSet()` schema → `resourceTencentCloudGa2GlobalAcceleratorAclRuleSetCreate/Read/Update/Delete` → `parseGa2GlobalAcceleratorAclRuleSetId` helper → `buildAclEntriesFromSchema` / `flattenAclRuleSetToSchema` / diff helpers.
- [x] 2.2 Schema fields per the spec, in this declaration order: `global_accelerator_id` (ForceNew), `global_accelerator_acl_policy_id` (ForceNew), `acl_entries` (`TypeList`, Required, `MinItems: 0`, Elem = resource with `protocol`/`port`/`source_cidr_block`/`policy`/`description`/`global_accelerator_acl_rule_id`), `task_id` (Computed). Add a `Timeouts` block with `Create/Update/Delete: 5 * time.Minute` defaults.
- [x] 2.3 Implement `Create`: if `acl_entries` is empty, skip the API call and set `d.SetId(gaId + FILED_SP + policyId)` then return `Read`. Otherwise build one `[]*ga2v20250115.AclEntries` slice from all `acl_entries` elements, call `CreateGlobalAcceleratorAclRuleWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil `Response` / nil-or-empty `GlobalAcceleratorAclRuleIds` / `len(ids) != len(input)` by returning `NonRetryableError`; log `[DEBUG]` with `logId` and `d.Id()` before the ID check; poll `TaskId` via `Ga2Service.WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutCreate))`; map `response.Response.GlobalAcceleratorAclRuleIds[i]` back onto `acl_entries[i].global_accelerator_acl_rule_id`; finally `d.SetId(gaId + FILED_SP + policyId)` (outside the retry block) and return `Read`.
- [x] 2.4 Implement `Read`: parse `d.Id()` into `gaId`/`policyId` via `parseGa2GlobalAcceleratorAclRuleSetId`; call `DescribeGa2GlobalAcceleratorAclRulesByPolicyId(ctx, policyId)`; if the returned set is empty, set `acl_entries` to an empty list (do NOT call `d.SetId("")`); log `[CRUD] ga2 global_accelerator_acl_rule_set id=<id>` to preserve context before any empty-result handling; otherwise sort the set by `global_accelerator_acl_rule_id` for deterministic state, flatten into `acl_entries` (setting `global_accelerator_acl_rule_id`, `protocol`, `port`, `source_cidr_block`, `policy`, `description` only when the response field is non-nil), and set `global_accelerator_id` / `global_accelerator_acl_policy_id`.
- [x] 2.5 Implement `Update`: on `d.HasChange("acl_entries")`, build old and new lists; diff keyed by `global_accelerator_acl_rule_id`: (a) new entries → batch-create via one `CreateGlobalAcceleratorAclRule` call (inside `resource.Retry(tccommon.WriteRetryTimeout, ...)`), poll `TaskId`, map returned IDs back; (b) removed entries → batch-delete via one `DeleteGlobalAcceleratorAclRule` call with all removed IDs, poll `TaskId`; (c) changed entries → call `ModifyGlobalAcceleratorAclRuleWithContext` per entry, poll each `TaskId`. If `global_accelerator_id` or `global_accelerator_acl_policy_id` changed, rely on ForceNew (Update is not reached). Set `task_id` to the last `TaskId` seen. Return `Read` at the end.
- [x] 2.6 Implement `Delete`: if `acl_entries` is empty, skip the API call and return nil. Otherwise collect all `global_accelerator_acl_rule_id` values into one `GlobalAcceleratorAclRuleIds` slice, call `DeleteGlobalAcceleratorAclRuleWithContext` inside `resource.Retry(tccommon.WriteRetryTimeout, ...)`; defend against nil `Response` / nil `TaskId`; poll `TaskId` via `Ga2Service.WaitForGa2TaskFinish(ctx, taskId, d.Timeout(schema.TimeoutDelete))`.
- [x] 2.7 At the top of every CRUD function, add `defer tccommon.LogElapsed("resource.tencentcloud_ga2_global_accelerator_acl_rule_set.<op>")()` and `defer tccommon.InconsistentCheck(d, meta)()`.
- [x] 2.8 Implement `parseGa2GlobalAcceleratorAclRuleSetId(id) (gaId, policyId string, err error)`: split by `tccommon.FILED_SP`, require exactly 2 non-empty parts; mirror the error-message style of the existing `parseGa2GlobalAcceleratorAclRuleId`.

## 3. Provider registration

- [x] 3.1 In `tencentcloud/provider.go`, register `"tencentcloud_ga2_global_accelerator_acl_rule_set": ga2.ResourceTencentCloudGa2GlobalAcceleratorAclRuleSet()` in `ResourcesMap`. Place it adjacent to the existing `tencentcloud_ga2_global_accelerator_acl_rule` entry to keep the `ga2` namespace contiguous.

## 4. Documentation example

- [x] 4.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule_set.md`. Content sections: short description (one sentence mentioning GA2); `Example Usage` HCL block showing `global_accelerator_id`, `global_accelerator_acl_policy_id`, and a multi-element `acl_entries` list (each element with `protocol`/`port`/`source_cidr_block`/`policy`/`description`); `Import` section showing `terraform import tencentcloud_ga2_global_accelerator_acl_rule_set.example ga-xxx#sp-xxx` and noting the composite `<global_accelerator_id>#<global_accelerator_acl_policy_id>` format. Do NOT add `Argument Reference` / `Attribute Reference` sections (auto-generated by `make doc`). Do NOT hand-edit `website/` files.

## 5. Unit test scaffolding

- [x] 5.1 Create `tencentcloud/services/ga2/resource_tc_ga2_global_accelerator_acl_rule_set_test.go`. Use the **gomonkey mock** approach (NOT the Terraform acceptance test suite) to mock the GA2 cloud API calls and unit-test the business logic. Cover at minimum: Create happy path (mock `CreateGlobalAcceleratorAclRule` returning IDs matching input length + a `TaskId`, mock `DescribeGlobalAcceleratorAclRules` for the post-create Read); Create with empty `acl_entries` (no API call); Update add/remove/modify diff paths; Delete happy path; the `parseGa2GlobalAcceleratorAclRuleSetId` error cases. Mock `Ga2Service.WaitForGa2TaskFinish` to return nil so tests stay fast. Ensure the generated code compiles under the current environment (do NOT run `go test`).

## 6. Build & verification (handled outside this proposal phase)

- [x] 6.1 Run `go build ./tencentcloud/...` and confirm clean compilation (executed by the verification flow, not in this openspec phase).
- [x] 6.2 Run `gofmt` formatting on the changed files (executed by the tfpacer-finalize skill).
- [x] 6.3 Run `make doc` to regenerate `website/docs/r/ga2_global_accelerator_acl_rule_set.html.markdown` from the resource Schema/Description and the `.md` example file (executed by the tfpacer-finalize skill). Do not hand-edit the generated file.
