## 1. Resource schema definition

- [x] 1.1 Create `tencentcloud/services/tag/resource_tc_tag_attachment_v2.go` with `ResourceTencentCloudTagAttachmentV2()` returning a `*schema.Resource` that has Create/Read/Update/Delete and an Importer (`schema.ImportStatePassthrough`).
- [x] 1.2 Define the schema fields: `tag_key` (Required, ForceNew), `tag_value` (Required, mutable), `resource` (Required, ForceNew). No additional query-filter fields are exposed because the Read path uses `GetResources`, which accepts the raw six-segment directly.

## 2. Service layer

- [x] 2.1 Add a `TagService` method `DescribeTagAttachmentV2ById(ctx, tagKey, resource)` in `service_tencentcloud_tag.go` that builds a `GetResources` request with `ResourceList = [resource]`, iterates `Response.ResourceTagMappingList` to find the mapping whose `Resource` matches, iterates its `Tags`, and returns the `*tag.Tag` whose `TagKey` equals `tagKey` (or nil if not found).

## 3. CRUD implementation

- [x] 3.1 Implement `resourceTencentCloudTagAttachmentV2Create`: build `AddResourceTagRequest` from `tag_key`/`tag_value`/`resource`, call `AddResourceTag` inside `tccommon.WriteRetryTimeout` retry (only the API call inside retry), check for nil/empty response and return `NonRetryableError`, set `d.SetId(strings.Join([]string{tagKey, resource}, tccommon.FILED_SP))` outside retry, then call Read.
- [x] 3.2 Implement `resourceTencentCloudTagAttachmentV2Read`: split composite ID into `tagKey`/`resource`, call the service-layer lookup, if no match log `[CRUD] tag_attachment_v2 id=%s` then `d.SetId("")`, otherwise set `tag_key`/`tag_value`/`resource` with nil checks before each `d.Set()`.
- [x] 3.3 Implement `resourceTencentCloudTagAttachmentV2Update`: split ID; if `tag_value` changed, build `UpdateResourceTagValueRequest` from stored `tag_key`/new `tag_value`/stored `resource`, call `UpdateResourceTagValue` inside `tccommon.WriteRetryTimeout` retry (API call only); `tag_key`/`resource` are ForceNew so no other mutable args; then call Read.
- [x] 3.4 Implement `resourceTencentCloudTagAttachmentV2Delete`: split ID into `tagKey`/`resource`, build `DeleteResourceTagRequest` with `TagKey` and `Resource`, call `DeleteResourceTag` inside `tccommon.WriteRetryTimeout` retry (API call only).

## 4. Provider registration

- [x] 4.1 Register `tencentcloud_tag_attachment_v2` in `tencentcloud/provider.go` (next to the existing `tencentcloud_tag` / `tencentcloud_tag_attachment` entries) mapping to `tag.ResourceTencentCloudTagAttachmentV2()`.

## 5. Documentation

- [x] 5.1 Create `tencentcloud/services/tag/resource_tc_tag_attachment_v2.md` with a one-line description mentioning Tag, Example Usage (HCL), and an Import section noting the composite ID `tag_key#resource` (joined by the field separator). Do NOT include `Argument Reference`/`Attribute Reference` (auto-generated). Use `jsonencode()` if any JSON-string fields arise.
- [x] 5.2 Update `tencentcloud/provider.md` to add the `tencentcloud_tag_attachment_v2` resource entry (this file is regenerated by `make doc` in the finalize phase).

## 6. Unit tests (gomonkey mocks, no TF acceptance suite)

- [x] 6.1 Create `tencentcloud/services/tag/resource_tc_tag_attachment_v2_test.go` using gomonkey to mock the tag client methods (`AddResourceTag`, `GetResources`, `UpdateResourceTagValue`, `DeleteResourceTag`) and the provider meta client; cover Create success + empty-response `NonRetryableError`, Read found/not-found, Update `tag_value` change, and Delete.
- [x] 6.2 Ensure all test functions check returned errors and use `_ = func()` for errors that are guaranteed nil, with no `go test` execution (build-only correctness).

## 7. Verification (separate phase)

- [x] 7.1 Run `gofmt` (deferred to the tfpacer-finalize phase) and ensure generated code compiles.
- [x] 7.2 Run `make doc` (deferred to the tfpacer-finalize phase) to generate `website/docs/r/tag_attachment_v2.html.markdown`.
